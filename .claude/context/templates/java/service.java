package ${PACKAGE};

import org.springframework.stereotype.Service;

/**
 * ${CLASS_NAME} - ${DESCRIPTION}.
 */
@Service
public class ${CLASS_NAME} {

    // Constructor injection
    public ${CLASS_NAME}(/* dependencies */) {
    }
}
