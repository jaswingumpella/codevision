# Defaults below point *both* local dev and Render deployments at the managed Render Postgres instance.
# If you want to run Postgres locally via Docker instead, comment these out and use the local block at the bottom.

SPRING_DATASOURCE_URL=jdbc:postgresql://dpg-d480qabipnbc73d6felg-a.oregon-postgres.render.com:5432/codevision_postgres
SPRING_DATASOURCE_USERNAME=codevision_postgres_user
SPRING_DATASOURCE_PASSWORD=N7f455H9K9YZxicckzibPgLF29fHU4h3
SPRING_DATASOURCE_MAX_POOL_SIZE=5
SPRING_DATASOURCE_MIN_IDLE=1
SPRING_JPA_HIBERNATE_DDL_AUTO=update

# --- Optional: run against a local Dockerized Postgres instead ---
# POSTGRES_DB=codevision
# POSTGRES_USER=codevision
# POSTGRES_PASSWORD=codevision
# POSTGRES_PORT=5432
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/codevision
# SPRING_DATASOURCE_USERNAME=codevision
# SPRING_DATASOURCE_PASSWORD=codevision
# SPRING_DATASOURCE_MAX_POOL_SIZE=10
# SPRING_DATASOURCE_MIN_IDLE=2
